export  const Dialog = ({isOpen }) => {
  // Adicione uma lógica para pegar um svg de acordo com algum tipo informado
    if (isOpen)
        return (
            <>
            <h1 >TESTE</h1>
            
            </>
        )
};
